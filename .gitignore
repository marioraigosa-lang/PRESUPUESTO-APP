# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dev-dist
*.local

# Variables de entorno (contienen llaves/secretos, nunca subir a git)
.env
.env.*
!.env.example

# Caché local de la CLI de Supabase (project ref, versiones, url del pooler
# de conexión a la base de datos): la regenera "supabase link"/"deploy" en
# cada máquina, no es parte del código -- mismo criterio que dist/dev-dist.
supabase/.temp

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
